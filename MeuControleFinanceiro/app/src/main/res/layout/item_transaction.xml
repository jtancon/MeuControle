<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingVertical="8dp"
    android:gravity="center_vertical">

    <!-- Ícone da Categoria -->
    <androidx.cardview.widget.CardView
        android:id="@+id/category_icon_background"
        android:layout_width="48dp"
        android:layout_height="48dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="0dp"
        android:backgroundTint="@color/red_light"> <!-- Cor de fundo padrão, será alterada via código -->

        <ImageView
            android:id="@+id/category_icon"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_shopping_bag"
            android:contentDescription="Ícone de Categoria"
            app:tint="@color/red_600" /> <!-- Cor do ícone padrão, será alterada via código -->
    </androidx.cardview.widget.CardView>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_marginStart="16dp">

        <TextView
            android:id="@+id/transaction_description"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/gray_800"
            android:textSize="16sp"
            android:textStyle="normal"
            tools:text="Compras de Supermercado" />

        <TextView
            android:id="@+id/transaction_category_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/gray_500"
            android:textSize="13sp"
            tools:text="Alimentação • Ontem, 18:30" />
    </LinearLayout>

    <TextView
        android:id="@+id/transaction_amount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/red_600"
        android:textSize="16sp"
        android:textStyle="bold"
        tools:text="- R$ 120,50" />

</LinearLayout>
